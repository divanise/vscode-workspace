#!/usr/bin/bash -e -o pipefail

DIRECTORY=$1

if [ -d $DIRECTORY ]; then
  for FILE in $DIRECTORY/*.sh; do
    if [ -f $FILE ]; then
      source $FILE
    fi
  done

  unset FILE
fi

unset DIRECTORY
